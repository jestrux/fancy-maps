const React = require('react');
require('./styles.css');

const Preview = ({}) => {

    return (
        <div id="Preview" className="flex-1 overflow-y-auto">
            <div style={{height: "1100px"}}>
                Ah yes!
            </div>
        </div>
    );
}

module.exports = Preview;